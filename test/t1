// imprime os numeros pares ate 'limite' e imprime calcula o sumatorio em sum
int i,sum,limite;
sum = 0;
limite = 10;
i = 0;
// comentarios
while(i < limite) {
    // modulo 
    if (i % 2){
        printi(i);
        sum = sum + 1;
    }
    i = i + 1;
}

printi(sum);
